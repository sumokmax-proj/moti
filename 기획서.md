# motimoti (모티모티) 기획서

동기부여가 되는 명언을 보여주는 모바일 웹앱

## 1. 개요

- **앱 이름**: motimoti (모티모티)
- **목적**: 사용자가 접속할 때마다 짧은 동기부여 명언을 보여주고, 버튼 클릭으로 새로운 명언을 랜덤하게 볼 수 있게 한다.
- **형태**: 모바일 웹앱 (반응형, 모바일 브라우저 우선)

## 2. 타겟 사용자

- 아침/휴식 시간에 짧은 동기부여가 필요한 일반 사용자
- 모바일 브라우저 사용자 중심 (데스크탑에서도 자연스럽게 동작)

## 3. 핵심 기능 (MVP)

- **명언 표시**: 첫 화면 중앙에 명언 본문 + 저자 표시
- **다음 명언**: 화면 하단 버튼 클릭 시 랜덤 명언으로 전환 (직전 명언과 중복되지 않도록 처리)
- **즐겨찾기 저장**: 하트/북마크 아이콘으로 현재 명언 저장, `localStorage`에 보관, 즐겨찾기 목록에서 확인 가능
- **공유하기**: 공유 버튼 클릭 시 Web Share API(모바일 지원) 또는 클립보드 복사로 명언 텍스트 공유

## 4. 화면 구성

### 메인 화면 (모바일 세로형 기준)

```
┌─────────────────────────┐
│        motimoti          │  ← 상단: 앱 타이틀
│                          │
│                          │
│   "명언 본문이 여기에      │  ← 중앙: 명언 카드
│    표시됩니다"            │     (본문 + 저자, 배경색/그라디언트)
│                          │
│         - 저자명 -        │
│                          │
│      ♡ 즐겨찾기  ⤴ 공유    │  ← 명언 카드 하단: 액션 아이콘
│                          │
│  ┌────────────────────┐  │
│  │    다음 명언        │  │  ← 하단: 다음 명언 버튼
│  └────────────────────┘  │
└─────────────────────────┘
```

### 즐겨찾기 화면 (서브 화면)

- 저장된 명언 리스트 (스크롤)
- 각 항목에서 삭제 가능

## 5. 사용자 흐름

1. 앱 접속 → 랜덤 명언 1개 표시
2. "다음 명언" 탭 → 새로운 랜덤 명언으로 전환 (페이드/슬라이드 애니메이션)
3. 즐겨찾기 아이콘 탭 → 현재 명언 저장 (아이콘 상태 변경으로 피드백)
4. 공유 아이콘 탭 → 공유 시트 또는 클립보드 복사 + 토스트 메시지
5. (선택) 즐겨찾기 화면 이동 → 저장된 명언 확인/삭제

## 6. 데이터 설계

- `quotes.js`(또는 `quotes.json`)에 명언 목록을 하드코딩
- 각 항목 구조 예시:

```js
{ id: 1, text: "명언 본문", author: "저자명" }
```

- 최소 30~50개 정도의 명언 목록으로 시작 (한국어 위주, 필요 시 영어 병기)
- 즐겨찾기 데이터는 `localStorage`에 저장된 `id` 배열(또는 명언 객체 배열)로 관리

## 7. 기술 스택

- 순수 **HTML + CSS + JavaScript** (프레임워크 없음, 빌드 도구 불필요)
- 모바일 반응형: viewport meta 태그 + flexbox/CSS로 중앙 정렬, 미디어 쿼리로 데스크탑 폭 제한
- 브라우저 API: `localStorage`(즐겨찾기), `navigator.share`(공유, 미지원 브라우저는 클립보드 복사로 폴백)
- 정적 파일이므로 별도 서버 설정 없이 바로 열람 가능 (GitHub Pages 등으로 배포 가능)

## 8. 비기능 요구사항

- 모바일 우선 반응형 디자인 (최소 320px 폭부터 대응)
- 명언 전환 시 부드러운 트랜지션(애니메이션)
- 빠른 로딩: 외부 API 의존 없음, 이미지 최소화

## 9. 향후 확장 아이디어 (MVP 범위 밖)

- 카테고리별 명언 (예: 도전, 성공, 인내)
- 다크모드
- PWA로 설치 가능하게 전환
- 외부 명언 API 연동으로 데이터 확장
