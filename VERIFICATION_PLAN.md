# 명언 출처 재검증 계획

> 이 문서는 새 세션에서 이어서 작업하기 위한 인수인계 문서다.
> Claude에게 "VERIFICATION_PLAN.md 따라서 재검증 진행해줘"라고 하면 된다.

## 배경

`quotes.js`의 명언 40개는 원래 **출처 확인 없이 모델 기억에서 생성된 데이터**였다. 실제로 문제가 드러났다.

- 40개 중 15개가 저자 "무명"
- id 2와 id 11이 같은 헨리 포드 명언의 중복
- id 5 "제이슨 카펠라" — 실존이 확인되지 않는 인물명
- 처칠·아인슈타인·디즈니 등 널리 알려진 오귀속 다수
- id 37 파스퇴르는 번역 과정에서 **원문과 다른 주장**으로 바뀌어 있었음
  (원문 "관찰의 영역에서 우연은 준비된 정신만을 돕는다" → "성공은 준비된 자에게 찾아오는 기회다")

1차 조사를 거쳐 **1차 출처가 확인된 21개**만 남기고, 각 항목에 `original`·`source`·`year`·`sourceUrl`을 기록하는 구조로 바꿨다.

## 왜 재검증이 필요한가

1차 조사 당시 세션은 클라우드 환경의 네트워크 등급이 **Trusted**여서 **WebFetch로 외부 사이트를 열 수 없었다.** 위키문헌·구텐베르크·Quote Investigator 등이 전부 프록시 CONNECT 단계에서 403으로 막혔다(기본 거부 허용목록 방식, DNS 문제 아님).

그래서 검증을 **WebSearch 결과 요약**에 의존했고, `sourceUrl`에는 검색 결과에 실제로 나타난 URL만 넣었다(추측한 주소는 없음). 하지만 **그 URL을 직접 열어 원문을 대조하지는 못했다.** 이 문서의 목적은 그 한 단계를 마저 채우는 것이다.

## 선행 조건 (완료되어 있어야 함)

claude.ai/code → 메시지 입력창 위의 클라우드 아이콘 → 환경(`Default`)의 톱니 아이콘 → **Network access**를 **Custom**으로 변경하고 아래 도메인 등록 (+ "Also include default list of common package managers" 체크) → 저장 후 **새 세션 시작**.

```
*.wikisource.org
*.wikiquote.org
*.wikipedia.org
www.gutenberg.org
quoteinvestigator.com
ctext.org
vangoghletters.org
news.stanford.edu
winstonchurchill.org
www.beethoven.de
```

환경 설정 변경은 **새 세션부터** 적용된다. 실행 중인 세션은 시작 시점 설정을 유지한다.

## 작업 순서

### 1. 접근 확인 (먼저 할 것)

위 도메인들에 WebFetch가 실제로 열리는지 확인한다. 여전히 막히면 도메인 표기를 고쳐 사용자에게 회신하고 **중단한다.** 막힌 채로 진행해서 "검증했다"고 하면 안 된다.

### 2. 기존 21개 재검증

각 항목의 `sourceUrl`을 실제로 열어, `original` 문자열이 그 페이지에 **문자 그대로** 있는지 대조한다.

| 결과 | 조치 |
|---|---|
| 원문 일치 | 유지 |
| 페이지는 열리나 해당 구절 없음 | 더 정확한 1차 출처 URL로 교체 |
| 1차 출처 끝내 확인 불가 | **삭제** (id 폐기, 재사용 금지) |

**우선 확인할 항목** (1차 조사에서 2차 출처에 의존했음):

- `id 30` 괴테 — `aphorismen.de`는 2차 출처. 『파우스트』 1부 2062행 원전으로 교체
- `id 49` 마리 퀴리 — wikiquote 경유. 1894.3.18 편지 또는 『피에르 퀴리』(1923) 원문으로
- `id 55` 에디슨 — wikiquote 경유. Harper's Monthly(1932) 또는 1927년 편지 근거로
- `id 41` 이순신 — ko.wikiquote 경유. 『난중일기』 원문 DB(한국고전종합DB 등)로
- `id 51` 반 고흐 — Quote Investigator 경유. `vangoghletters.org`의 **실제 편지 번호**로 교체

### 3. 원문(original) 보강

`original`이 원어가 아닌 항목을 원어로 채운다.

- `id 47` 소로, `id 53` 더글러스 — 영어 원문 원전에서 확인
- 그리스어·라틴어가 필요한 항목은 원전에서 확보 (없으면 등재하지 않음)

### 4. 확장 (21개 → 30개 목표)

원전 직접 열람이 가능해지면 **source-first** 방식으로 추가한다.

> **원칙: 기억에서 문구를 떠올려 검색하지 않는다.**
> 구텐베르크·위키문헌·한국고전종합DB에서 **텍스트를 먼저 읽고, 읽은 것 중에서 고른다.**
> 읽고 있는 문장은 지어낼 수 없다. 이 순서가 이 작업의 핵심이다.

보강 방향:

- **여성 인물** (현재 3명뿐: 마리 퀴리·헬렌 켈러·나이팅게일) — 로자 파크스(자서전), 에밀리 디킨슨(시), 아멜리아 에어하트(『The Fun of It』) 등 1차 출처가 확인되는 인물만
- **한국·동아시아 원전** — 『난중일기』 추가 구절, 『목민심서』, 『백범일지』 등

### 5. id 규칙 (반드시 지킬 것)

`app.js`의 `toggleFavorite`이 `q.id === quote.id`로만 매칭해 splice하므로(app.js:38-40), **id를 재사용하면 사용자가 저장해둔 무관한 명언이 조용히 삭제된다.**

- 생존 항목의 id는 **고정**
- 신규 항목은 **56번부터**
- 삭제된 id는 **절대 재사용 금지**
- `quotes.js` 헤더의 "삭제된 id / 다음 id" 목록을 갱신할 것

## 수정 대상

- `quotes.js` — **유일한 수정 파일**
- `app.js` / `style.css` / `index.html` 은 건드리지 않는다 (출처는 데이터에만 저장, 화면 표시 안 함)

`app.js`는 `id`·`text`·`author`만 읽으므로 나머지 필드는 렌더링에 영향이 없다.

## 검증

1. `node --check quotes.js`
2. **스키마 검사** — 필수 필드(`id`/`text`/`author`/`original`/`lang`/`source`/`year`/`sourceUrl`) 존재, id 유일성, 삭제 id 미재사용, 저자 중복 없음, `text` 60자 이하, `text`에 외곽 따옴표 없음, 배열 비어있지 않음
3. **실제 구동** — `python3 -m http.server` + Playwright 헤드리스(320px)
   - chromium 실행 경로: `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`
   - playwright 모듈: `/opt/node22/lib/node_modules/playwright`
   - 확인 항목: 명언 전환(직전과 중복 안 됨), 즐겨찾기 저장/새로고침 유지/삭제, 공유 폴백 토스트, 가로 넘침 없음, 최장 문구 클리핑 없음
4. **레거시 즐겨찾기** — 삭제된 id(예: 5번)를 localStorage에 미리 심고, 스냅샷으로 정상 표시되며 하트 오점등·오삭제가 없는지 확인
5. **출처 스팟체크** — 전 항목의 `sourceUrl`을 실제로 열어 `original` 존재를 확인했음을 근거로 보고 (이번에는 예외 없이)
6. `claude/motivational-quote-app-7f4hr6` 브랜치에 커밋·푸시 → GitHub Pages 자동 재배포

## 수록 기준 (변경 금지)

`quotes.js` 상단 주석과 동일하다.

- **1차 출처**(책/연설/편지/당대 기록)로 확인된 명언만 등재
- 모든 항목에 `original`·`lang`·`source`·`year`·`sourceUrl` 필수
- 출처를 제시할 수 없으면 등재하지 않음
- **애매하면 탈락시킨다.** "널리 알려졌지만 출처 불명"은 예외 없이 제외
- 한국어 `text`는 반드시 `original`에서 번역. 원문의 주장(주어·서술어·한정 조건)을 바꾸는 의역 금지
- 길이 때문에 원문 주장을 훼손해야 한다면, 줄이지 말고 **그 항목을 뺀다**

## 참고: 1차 조사에서 걸러낸 오귀속

같은 항목을 다시 넣지 않도록 기록해 둔다.

| 명언 | 실제 |
|---|---|
| 위고 "가장 어두운 밤도 끝나고 태양은 떠오른다" | 소설이 아니라 **뮤지컬 가사**(Herbert Kretzmer) |
| 만델라 "불가능해 보이지만 해내고 나면…" | Mandela Foundation·Quote Investigator 모두 **기록 없음** |
| 미켈란젤로 "Ancora imparo" | 1806년 Duppa가 타인의 작품을 오식별한 데서 유래 |
| 에디슨 "실패한 사람 다수는 성공 직전에 포기…" | 1차 출처 없음 (집계 사이트에만 존재) |
| 아멜리아 에어하트 "가장 효과적인 방법은 그냥 하는 것" | 1차 출처 없음 |
| 마야 안젤루 "많은 패배를 겪더라도…" | 출판물 위치 특정 불가 (다큐멘터리 인용만) |
| 그레츠키 "시도하지 않으면 100% 실패" | 1983년 기록은 있으나 동시기 타인 사용례와 겹쳐 보류 |
